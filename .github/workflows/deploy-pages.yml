name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      # VITE_* pri buildu: .env.production u repou (VITE_BASE_PATH=/) + eksplicitno ispod (spriječi stari /MOJPUT u org secrets).
      - name: Build app
        run: npm run build
        env:
          VITE_BASE_PATH: /

      - name: Configure Pages
        uses: actions/configure-pages@v5
        with:
          # Omogući Pages na repou ako još nije uključeno (inače "Get Pages site" → 404 Not Found).
          enablement: true

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

  deploy:
    if: github.ref == 'refs/heads/main'
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
